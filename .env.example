# Database connection. Example for PostgreSQL:
DATABASE_URL="postgresql://username:password@localhost:5432/invoicedb"

# JWT secret used to sign tokens. Replace with a strong random string in production
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Public app URL (used for callbacks, absolute links, etc.)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional: Quick local testing with SQLite. Uncomment to use a local file DB
# DATABASE_URL="file:./dev.db"
